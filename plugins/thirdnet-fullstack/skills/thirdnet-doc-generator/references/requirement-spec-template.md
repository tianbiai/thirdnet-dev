# [项目名称] 需求规格说明书

> 项目级需求规格说明。明确系统功能边界、角色权限、业务流程与验收标准，作为开发、测试与验收的依据。
> 占位符（`[...]`）由 thirdnet-doc-generator 技能填充；第 4 节按实际模块复制扩充（每个模块包含：模块说明 / 主要功能 / 业务流程 / 业务规则 / 注意事项）。最终文档请删除所有「填写说明」引用块。

**文档版本**：[v1.0]　**编写日期**：[YYYY-MM-DD]　**编写者**：[git user / 姓名]

---

## 1. 引言

### 1.1 编写目的

[说明本文档的受众（开发 / 测试 / 业务方 / 验收方）与用途。]

### 1.2 项目背景

[项目立项背景、解决的核心问题、业务价值。代码无法推导，需向用户确认。]

### 1.3 术语与缩写

| 术语 | 说明 |
|------|------|
| [术语1] | [说明] |

### 1.4 参考资料

- [相关业务规范 / 上游政策文件 / 关联系统文档]

---

## 2. 总体描述

### 2.1 产品定位

[一句话定位 + 核心目标用户。]

### 2.2 技术栈

.NET 10 + PostgreSQL + EF Core + Redis + JWT（国密）；前端 Vue 3 + Element Plus（Web）、uni-app + Vant（小程序）。

### 2.3 运行环境

| 端 | 环境 |
|----|------|
| 后端 | [.NET 运行时版本 / 操作系统 / 数据库版本] |
| 前端 Web | [现代浏览器最低版本] |
| 小程序 | 微信小程序基础库 [版本] |

---

## 3. 角色与权限

> 用角色与业务语言描述"谁能做什么"，不写权限串等代码细节。

### 3.1 角色清单

| 角色 | 职责定位 |
|------|---------|
| [角色1，如：系统管理员] | [职责] |
| [角色2，如：业务员] | [职责] |

### 3.2 权限矩阵

| 功能 \ 角色 | [角色1] | [角色2] |
|------------|---------|---------|
| [功能1] | ✅ / ❌ | ✅ / ❌ |

---

## 4. 功能性需求

> 每个功能模块作为一个二级章节，完整描述该模块的**功能、业务流程、业务规则与注意事项**，让评审 / 开发 / 测试 / 业务方准确理解模块要做什么。模块边界源自代码扫描（Vue 页面 + Controller），用户在 Step 3 确认裁剪。
>
> **粒度边界**：本节描述"系统应提供什么能力、业务如何流转、受哪些规则约束"；具体的操作步骤、输入输出与预期响应属于**测试用例文档**，不在本节展开。

### 4.1 [模块1]

**模块说明**：[模块用途与业务定位，一句话说清这个模块为什么存在、服务谁]

**主要功能**：

- **[功能1，如：通知查询]**（P0）：[提供什么能力、解决什么问题、典型使用场景]
- **[功能2，如：通知发布]**（P1）：[...]

**业务流程**：

[该模块核心业务流转，按业务事件顺序描述。如：发起通知 → 选择接收范围 → 提交审核 → 审核通过 → 发布 → 推送 → 接收人已读统计。可用文字或简单流程示意，停留在业务事件粒度，不写界面操作步骤。]

**业务规则**：

- [规则1：字段取值范围 / 必填 / 长度格式，源自枚举字典与校验逻辑]
- [规则2：状态机流转、唯一性约束、可见性 / 权限要求等]

**注意事项**：

- [边界情况、与其他模块的依赖、已知限制、需评审方重点关注的事项]

### 4.2 [模块2]

[同上结构复制]

---

## 5. 非功能性需求

| 类别 | 指标 | 要求 |
|------|------|------|
| 性能 | 接口响应时间 | [如 P95 < 500ms] |
| 性能 | 分页查询 | [如单表千万级 keyset 分页] |
| 安全 | 认证 | JWT（国密 SM2）+ Basic Auth + API-Key |
| 安全 | 授权 | 基于角色的细粒度权限控制 |
| 安全 | 数据 | ORM 参数化防 SQL 注入；HTTPS 强制 |
| 可用性 | [可用率] | [如 99.9%] |
| 兼容性 | 浏览器 | [Chrome / Edge / Safari 最近 N 个版本] |

> 数值类指标代码无法确定，须向用户确认，不要编造。

---

## 6. 数据需求

> 描述核心数据对象与业务字典，不展开表结构与字段类型（细节见 `spec.md`）。

### 6.1 核心数据对象

| 数据对象 | 业务含义 | 关键属性概念 |
|---------|---------|------------|
| [对象1，如：通知] | [含义] | [如：标题、内容、类型、状态、接收范围] |

### 6.2 字典 / 枚举

| 字典 / 状态 | 取值 | 业务含义 |
|------------|------|---------|
| [如：通知类型] | 系统 / 业务 | [含义] |

---

## 7. 接口需求

> 以功能为单位说明需要哪些系统能力，不写路由与权限串。

| 接口能力 | 用途 / 触发场景 | 输入概念 | 输出概念 |
|---------|---------------|---------|---------|
| [如：查询通知列表] | [进入列表页] | [筛选条件 + 分页] | [通知条目集合] |
| [如：创建通知] | [保存草稿] | [通知内容] | [创建结果] |

---

## 8. 约束与假设

### 8.1 约束

- [如：网关限制仅 GET/POST；认证走 ThirdNet AdminService 而非 IdentityServer]

### 8.2 假设

- [如：默认部署于内网；某外部系统数据每日同步一次]

---

## 9. 验收标准

| 编号 | 验收项 | 验收标准 | 对应功能 |
|------|--------|---------|---------|
| AC-001 | [验收项] | [可观测、可度量的标准] | [模块]-[功能名] |

---

## 10. 备注

[扫描阶段发现的代码与既有 spec/plan 差异、待确认事项等。]
