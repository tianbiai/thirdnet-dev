# [项目名称] 测试用例文档

> 测试执行、回归与验收依据。用例以**业务行为**为粒度——围绕"用户能完成什么操作、系统应如何响应"组织。
> 边界 / 异常用例源自业务规则与字段约束；不堆砌路由与类型签名。占位符由 thirdnet-doc-generator 填充；用例行按实际功能点复制扩充。

**文档版本**：[v1.0]　**编写日期**：[YYYY-MM-DD]　**编写者**：[git user / 姓名]

---

## 1. 测试范围

### 1.1 测试目标

[本次测试覆盖的功能模块与目的。]

### 1.2 测试范围

| 范围   | 说明         |
| ------ | ------------ |
| 包含   | [模块清单]   |
| 不包含 | [明确排除项] |

### 1.3 测试环境

| 项       | 配置                                |
| -------- | ----------------------------------- |
| 后端     | [服务地址 / 版本]                   |
| 前端     | [Web 地址 / 小程序版本]             |
| 数据库   | [PostgreSQL 版本]                   |
| 测试账号 | [角色与账号，如管理员 / 业务员各一] |

---

## 2. 功能测试用例

> 每条用例聚焦一个可观测的验证点。每步按「操作 → 预期」书写，自上而下可顺序执行。
> 用例编号建议 `TC-{模块}-{序号}`；用例行按实际功能点复制扩充。

| 用例编号      | 模块   | 用例标题          | 前置条件               | 测试步骤                                                                                              |
| ------------- | ------ | ----------------- | ---------------------- | ----------------------------------------------------------------------------------------------------- |
| TC-[模块]-001 | [模块] | [正常新增]        | 已登录且有相应操作权限 | 1. 进入[模块]列表页 → 页面正常加载<br>2. 点击「新增」→ 弹出新增表单<br>3. 依次填写各字段合法值 → 无校验报错<br>4. 点击「保存」→ 提示成功，列表刷新并出现新记录 |
| TC-[模块]-002 | [模块] | [必填校验]        | 同上                   | 1. 点击「新增」打开表单<br>2. 将[必填字段]留空<br>3. 点击「保存」→ 提示「[字段]不能为空」，表单不提交                                      |
| TC-[模块]-003 | [模块] | [长度 / 格式校验] | 同上                   | 1. 点击「新增」打开表单<br>2. 在[字段]输入超长 / 非法格式内容<br>3. 点击「保存」→ 提示对应校验错误，不提交                                |
| TC-[模块]-004 | [模块] | [枚举边界]        | 同上                   | 1. 点击「新增」打开表单<br>2. 对[枚举字段]依次选择每个选项并保存<br>3. 重新打开该记录 → 各值均可正常保存与回显                          |
| TC-[模块]-005 | [模块] | [无权限访问]      | 登录无该操作权限的账号 | 1. 进入[模块]菜单 / 点击[操作按钮]<br>2. 观察入口与操作 → 入口不可见，或操作被拒并提示无权限                                            |
| TC-[模块]-006 | [模块] | [删除二次确认]    | 列表已有数据           | 1. 选中一条记录点击「删除」→ 弹出二次确认框<br>2. 点击「取消」→ 确认框关闭、记录仍存在<br>3. 再次「删除」并「确认」→ 记录删除，列表刷新 |
| TC-[模块]-007 | [模块] | [分页 / 查询]     | 数据 > 1 页            | 1. 进入列表 → 默认加载第 1 页，分页信息正确<br>2. 切换到第 2 页 → 数据与分页状态更新<br>3. 输入筛选条件查询 → 结果按条件过滤            |

> 边界用例源自业务规则与字段约束（必填 / 长度 / 选项）；权限分支用例源自角色与功能的可见性。

---

## 3. 接口测试用例

> 以接口能力为单位描述测试场景（系统对外只提供查询与提交两类操作），关注输入场景与预期行为，不写路由与类型签名。

| 用例编号   | 接口能力           | 输入场景             | 预期行为                 |
| ---------- | ------------------ | -------------------- | ------------------------ |
| TC-API-001 | [如：查询通知列表] | 合法的筛选条件与分页 | 正常返回结果集合         |
| TC-API-002 | [如：查询通知列表] | 缺少必填的筛选条件   | 提示参数缺失，不返回数据 |
| TC-API-003 | [如：创建通知]     | 各字段合法           | 创建成功                 |
| TC-API-004 | [如：创建通知]     | 未登录调用           | 拒绝访问（未认证）       |
| TC-API-005 | [如：修改通知]     | 当前角色无修改权限   | 拒绝访问（无权限）       |
| TC-API-006 | [如：删除通知]     | 目标不存在           | 提示不存在               |

---

## 4. 测试矩阵

> 模块 × 用例数概览，便于评估覆盖度。

| 模块           | 功能用例 | 接口用例 | 合计 |
| -------------- | -------- | -------- | ---- |
| [模块1]        | [n]      | [n]      | [n]  |
| **合计** | [n]      | [n]      | [n]  |

---

## 5. 缺陷跟踪

| 缺陷编号 | 模块   | 描述   | 严重程度           | 状态                   | 负责人 |
| -------- | ------ | ------ | ------------------ | ---------------------- | ------ |
| BUG-001  | [模块] | [描述] | 严重 / 一般 / 轻微 | 新建 / 修复 / 验证通过 | [姓名] |

---

## 6. 备注

[测试数据准备说明、待补充用例清单、扫描阶段发现的边界规则等。]
