{
  "skill_name": "thirdnet-digital-twin",
  "evals": [
    {
      "id": 1,
      "name": "text-interview-new-park",
      "prompt": "帮我给『滨江科创园』做一个智慧运行驾驶舱的3D数字孪生大屏。园区有三栋楼：研发楼12层、办公楼6层、会展中心3层，还有一个地下车库大概400个车位差不多九成满。配色就用现在的赛博蓝。先别急着写一堆代码——我想先看到你整理出来的园区规格（每栋楼的尺寸、位置、类别、车库占用、图例、楼幢切换标签），确认没问题再生成场景。",
      "expected_output": "A validated Park Spec JSON for 滨江科创园 with 3 buildings (研发楼 building 12F, 办公楼 building 6F, 会展中心 building 3F) + one garage-category building (v1.3: no capacity/occupied in spec) + legend (楼幢/地下车库) + switcher tabs (全局视角/研发楼/办公楼/会展中心/地下车库), written to the output dir. Garage occupancy (capacity 400, ~360 occupied, ~40 empty) is captured as a parking POI in the dynamic mock data (v1.5). Followed by: static scaffold src/data/<park>.ts (footprint geometry only) + the 5-file IDigitalTwinApi contract layer (mock derived from spec) + ParkScene.ts wiring grid.glsl floor + category-colored buildings + garage entrance, with onMounted hydration.",
      "files": [],
      "expectations": [
        "Produces a spec.json that passes scripts/validate_spec.py (exit 0)",
        "Spec has exactly 3 non-garage buildings + 1 garage-category building",
        "All three non-garage buildings are category:building",
        "Spec has NO garage.capacity/occupied fields (v1.3 removed); the 400/~360/~40 occupancy lives in mockPois as a parking-category POI's occupancy (v1.5)",
        "Generated ParkScene.ts imports gridGround.glsl?raw and wires a ShaderMaterial floor",
        "Buildings are colored by category (cyan/mint) from tokens, not hardcoded",
        "Switcher list includes 全局视角 and 地下车库",
        "Generated ParkScene.ts derives the ortho frustum from spec geometry with content fill K≈0.66 (park occupies ~2/3 of the frame, surrounding roads/greenery visible), instead of the v1.1 K=0.88 close-up",
        "Generated scene builds the §10 park environment (internal loop road + surface parking + street trees + surrounding municipal roads + wall + gate + street lamps), driven by spec.environment or smart defaults",
        "Generated 5-file IDigitalTwinApi contract layer: api/types/digital-twin.ts + api/interfaces/(manager/)digital-twin.ts + api/modules/manager/digital-twin.ts + mock/api/manager/digital-twin.ts + mock/data/manager/digital-twin.ts (v1.5)",
        "IDigitalTwinApi has getBuildings/getFloorDetail/getPois; createDigitalTwinApi() switches Real/Mock by VITE_MOCK_ENABLED (v1.5)",
        "mockBuildings/mockPois are derived from spec (研发楼/办公楼/会展中心 names+floors; parking POI with occupancy 400/~360/~40) (v1.5)",
        "GlobalTwin.vue onMounted calls getBuildings()+getPois() and hydrates the scene; static scaffold (footprint+environment) renders before hydration; floor click calls getFloorDetail() (v1.5)",
        "Static scaffold src/data/<park>.ts contains ONLY footprint geometry (id/w/d/x/z/category/facing) — NOT building names/floors/POIs (v1.5)",
        "Generated useSelection.ts exposes effBuildingId/effFloorIndex computed (hover ?? focused) AND a selectFloor(b, f) that writes focusedBuildingId + floorIndex atomically in one call (v1.5.1)",
        "Generated ParkScene floor-click handler (pointerup) calls ONLY onSelect(bid, fin) on the click path — does NOT also invoke onFocus/focusBuilding (which would clear floorIndex and make the gold border vanish after mouseleave) (v1.5.1)",
        "Generated GlobalTwin.vue drives the gold overlay via watch([effBuildingId, effFloorIndex]) -> scene.setSelection, so a selected floor keeps its gold border after the mouse leaves the canvas (v1.5.1)"
      ]
    },
    {
      "id": 2,
      "name": "pen-design-source",
      "prompt": "用项目根目录的 数字孪生.pen 这个设计稿，把全局视角的3D数字孪生场景按稿子还原出来。现在代码里那个地面是深色沥青贴图、不是设计稿里的发光网格，图例也没了——要按赛博风格修好。先把规格从设计稿里抽出来给我看。",
      "expected_output": "Runs scripts/extract_pen.py on 数字孪生.pen to draft a spec (tokens, grid uniforms u_cell=46/u_gridColor=#2a7fff/u_strength=0.85, buildings 综合楼5F/主楼10F/服务楼3F; v1.3 extractor no longer reads garage occupancy), fills id/category/w/d/x/z from the Scene, generates the cyber-spec-correct scene wiring the grid shader. Garage/lot occupancy (if the .pen sign shows 320/96) is captured via interview/default as a parking POI occupancy in mockPois (v1.5). All outputs saved to the output dir, including the 5-file IDigitalTwinApi contract layer.",
      "files": [],
      "expectations": [
        "Produces a spec.json derived from 数字孪生.pen (title contains 智慧运行驾驶舱, 3 buildings from the headers)",
        "Spec shaders.grid matches the .pen fill uniforms (u_gridColor #2a7fff, u_cell 46, u_strength 0.85)",
        "Spec has NO garage.capacity/empty fields (v1.3); any 320/96 occupancy is a parking POI occupancy in mockPois, not in spec (v1.5)",
        "All non-garage buildings are category:building",
        "Generated ParkScene.ts wires the grid.glsl ShaderMaterial floor",
        "Generated scene includes a garage entrance (half-pyramid + P sign) with NO occupancy sign/progress bar in 3D (v1.3); occupancy shown via parking POI tooltip from getPois() (v1.5)",
        "Generated scene default framing shows the full park panorama (K≈0.66) and includes the §10 environment (roads/greenery/surrounding) from the .pen decorative layers",
        "Generated IDigitalTwinApi contract layer (5 files) with mock data derived from the spec; RealDigitalTwinApi calls /api/manager/park/* (v1.5)"
      ]
    },
    {
      "id": 3,
      "name": "fix-extend-existing",
      "prompt": "现在的数字孪生大屏有几个问题想修：1）地面是深色沥青贴图、不是发光网格；2）没有图例，楼的颜色没按类别（楼幢/地下车库）区分；3）楼幢切换里没有『地下车库』这一项。帮我按赛博风格把这些修好。给出改动的关键文件就行。",
      "expected_output": "Identifies the exemplar gaps (shader not wired, no legend, no category coloring, missing 地下车库 tab) and produces the corrected key files (ParkScene.ts with shader floor + category colors + legend overlay, the garage entrance, and the 5-entry switcher list), saved to the output dir, without rewriting the whole app.",
      "files": [],
      "expectations": [
        "Output diagnoses the 3 stated problems (shader, legend/category, 地下车库 tab)",
        "Corrected ParkScene.ts imports and wires gridGround.glsl (ShaderMaterial floor)",
        "Corrected scene colors buildings by category and includes a Legend overlay with 楼幢/地下车库",
        "Corrected switcher list has 5 entries including 地下车库",
        "Does not reintroduce hardcoded park content — references a spec/tokens"
      ]
    },
    {
      "id": 4,
      "name": "environment-richer-default",
      "prompt": "给『云栖智造园』做个数字孪生大屏，赛博风格就行。园区有 4 栋楼和一个地下车库，具体你看着安排。我想看到的是一张能看全整个园区和周边环境的全景大图，不要那种把楼怼到镜头前的特写。",
      "expected_output": "A validated Park Spec for 云栖智造园 (4 buildings + 1 garage, cyber style). Because the user gave no environment details, spec.environment is either omitted (smart defaults) or populated with sensible defaults — in either case the generated scene is NOT a bare 'buildings + empty ground' anymore. The scene includes internal roads, surface parking, street trees / green patches, surrounding municipal roads, a perimeter wall + main gate, and street lamps; cyber ground-glow markings on. Default framing is a panorama: park content fills ~2/3 of the frame (K≈0.66) with surrounding roads/greenery/wall visible around it, and the wheel can zoom out further (~0.45). Dynamic data (building names/floors, POIs) goes through the IDigitalTwinApi contract layer with mock data derived from spec.",
      "files": [],
      "expectations": [
        "Produces a spec.json that passes scripts/validate_spec.py (exit 0); environment is either omitted or a valid object",
        "Generated ParkScene.ts frames the park at K≈0.66 (park ~2/3 of frame, surrounding environment visible) — NOT the v1.1 K=0.88 close-up",
        "OrbitControls zoom clamp min is ~0.45 so the user can zoom out to see the whole panorama",
        "Generated scene builds internal roads (loop/cross/grid) inside the boundary",
        "Generated scene includes surface parking stalls (with vehicle proxies matching occupancy) — count derived from building scale when not specified",
        "Generated scene includes greenery: grass patches + street trees (trunk + canopy), density 'normal' by default",
        "Generated scene includes surrounding municipal roads on all 4 sides + perimeter wall + main entrance gate",
        "Generated scene includes street lamps; cyber ground-glow markings (Line2 emissive road lines) are visible",
        "Environment meshes do NOT enter pickables[] — building floor raycast selection still works correctly",
        "All environment colors come from the cyber theme tokens environment block, not hardcoded hex",
        "Generated IDigitalTwinApi contract layer (5 files); GlobalTwin.vue hydrates buildings+POIs on mount via getBuildings()+getPois(); camera framing uses static geometry only (does not wait for hydration) (v1.5)"
      ]
    },
    {
      "id": 5,
      "name": "dynamic-data-via-api",
      "prompt": "我们这个园区数字孪生大屏，现在楼名、楼层、设备/监控/停车场点位都是写死在前端页面里的，运维那边希望这些数据从后端 API 实时拿。基本不变的东西（楼的位置、园区道路绿化、配色）还放页面里就行。开发阶段先用 mock 数据跑通，正式上线接真实后端。帮我按这个思路改。",
      "expected_output": "Refactors the digital twin so data is layered: basic info (building footprint geometry, environment, theme tokens, stage) stays statically inlined; dynamic data (building names/floor counts/floor_ids, floor detail on click, POI points incl. devices/cameras/parking with realtime status + occupancy) flows through an IDigitalTwinApi contract layer following api-typescript-spec (5 files + Real/Mock factory, VITE_MOCK_ENABLED switch). Page uses a scaffold-first-then-hydrate load sequence. Includes a backend contract doc listing the 3 endpoints the backend must implement. Outputs the contract-layer files + the hydration wiring in GlobalTwin.vue, saved to the output dir.",
      "files": [],
      "expectations": [
        "Output clearly separates basic (static) vs dynamic (API) data in a table or explicit list, matching the v1.5 partition (geometry/environment/tokens = static; names/floors/floor-detail/POIs = dynamic)",
        "Generated src/api/types/digital-twin.ts defines BuildingRuntimeItem, FloorDetail (+UnitDetail), PoiRuntimeItem, PoiStatusEnum, PoiTypeEnum and the *Params types — all snake_case",
        "Generated src/api/interfaces/(manager/)digital-twin.ts defines IDigitalTwinApi with getBuildings(): Promise<BuildingRuntimeItem[]>, getFloorDetail(params): Promise<FloorDetail>, getPois(params?): Promise<PoiRuntimeItem[]> (GET-only, no response envelope)",
        "Generated src/api/modules/manager/digital-twin.ts has RealDigitalTwinApi (calling /api/manager/park/buildings, /floor-detail, /pois) + createDigitalTwinApi() factory switching by MOCK_ENABLED + module singleton digitalTwinApi",
        "Generated src/mock/api/manager/digital-twin.ts (MockDigitalTwinApi) + src/mock/data/manager/digital-twin.ts (mock data derived from the spec: building names/floors, POIs with status + parking occupancy)",
        "createDigitalTwinApi() branches on VITE_MOCK_ENABLED (string 'true'/'false', NOT VITE_USE_MOCK); .env sets true for dev, .env.production sets false for prod (or reuses admin template .env)",
        "GlobalTwin.vue onMounted does Promise.all([getBuildings(), getPois()]) then scene.hydrateBuildings()/hydratePois(); floor click calls getFloorDetail(); loading/empty/error states are handled (scaffold stays interactive on failure)",
        "Static scaffold (src/data/<park>.ts) contains ONLY building footprint geometry (id/w/d/x/z/category/facing) — building names/floors and POIs are NOT statically inlined",
        "Camera framing (frameCamera) uses only static geometry and does NOT wait for dynamic data (no white screen pre-hydration)",
        "Output includes a backend contract doc (or section) listing the 3 GET endpoints with route, QueryMap, returned *Map DTO (snake_case), permission string, and Controller name — front-first, aligned with net-api-developer",
        "Adapts to host: if src/api/request.ts + src/config exist (admin template) they are reused and the interface file is flat (interfaces/digital-twin.ts); otherwise a minimal fetch-based request.ts + config + .env are generated"
      ]
    },
    {
      "id": 6,
      "name": "blueprint-style-park",
      "prompt": "给『中建科创谷』做个数字孪生大屏。我们这是工程/产业园，想要那种工程蓝图的感觉——深蓝图底配白线框楼栋和坐标网格，专业、技术范儿，不要花哨的霓虹也不要写实。园区 3 栋楼（研发楼 8F、中试楼 5F、展示中心 3F）+ 一个地下车库，尺寸你安排。先生成规格给我确认。",
      "expected_output": "A validated Park Spec for 中建科创谷 with style='blueprint' (3 buildings 研发楼/中试楼/展示中心 + 1 garage). The generated scene wires gridGround.glsl as the floor ShaderMaterial with BLUEPRINT uniforms (deep-blue ground + pale white-blue coordinate grid, from assets/themes/blueprint.tokens.json shaders.grid), uses flat MeshBasicMaterial with low-opacity blue fill + white EdgesGeometry/Line2 wireframe-dominant buildings, NoToneMapping, shadowMap disabled. Category colors from blueprint tokens. Plus the standard shared parts (roof name labels, garage half-pyramid entrance + P sign, surface parking stalls with P, legend, K≈0.66 panorama framing) and the 5-file IDigitalTwinApi contract layer (v1.5) with mock data derived from spec.",
      "files": [],
      "expectations": [
        "Produces a spec.json that passes scripts/validate_spec.py with style='blueprint' (exit 0)",
        "Spec style is 'blueprint' (the new engineering-blueprint style, NOT cyber/realistic/night-realistic; 'minimal' is removed and must NOT be offered)",
        "Generated ParkScene.ts imports gridGround.glsl?raw and wires a ShaderMaterial floor — blueprint (like cyber) consumes the grid shader, unlike the other 5 styles which skip it",
        "Grid ShaderMaterial uniforms come from assets/themes/blueprint.tokens.json shaders.grid (u_gridColor pale white-blue ~#cfe4ff, u_cell 46, u_strength ~0.6) — NOT the cyber neon-cyan uniforms (#2a7fff/0.85)",
        "Buildings use flat MeshBasicMaterial with transparent low-opacity fill + white EdgesGeometry/Line2 outlines (wireframe-dominant technical-drawing look), NOT PBR/Standard materials",
        "Renderer uses NoToneMapping and shadowMap is disabled (blueprint is a flat engineering-drawing aesthetic, not lit)",
        "All colors derive from assets/themes/blueprint.tokens.json — no hardcoded hex, no neon palette leaking from cyber",
        "Standard shared parts present: per-roof name labels, garage half-pyramid entrance + P sign, surface parking stalls each stamped P, Legend overlay, K≈0.66 panorama framing with surrounding environment",
        "Label contrast (§4.2) follows deep-blue-bg + pale-white-blue text (亮字深底), readable on the blueprint ground",
        "Generated 5-file IDigitalTwinApi contract layer (v1.5) with mock data derived from spec (研发楼 8F / 中试楼 5F / 展示中心 3F); GlobalTwin.vue hydrates via getBuildings()+getPois()"
      ]
    },
    {
      "id": 7,
      "name": "realistic-style-park",
      "prompt": "给『湖畔科创中心』做个数字孪生大屏，要日间真实感——真实建筑可视化、玻璃幕墙反射、柔和阳光阴影，不要那种霓虹仪表盘。园区 3 栋楼（A 座 12F、B 座 8F、C 座 4F）+ 一个地下车库，尺寸你安排。先生成规格给我确认。",
      "expected_output": "A validated Park Spec with style='realistic' (3 buildings + 1 garage). Generated scene uses ACESFilmicToneMapping + outputColorSpace=SRGB + PCFSoftShadowMap; pre-baked HDR/RoomEnvironment environment (v1.8: NOT runtime Sky+PMREM); DirectionalLight sun from tokens.lights; concrete/road bodies use MeshLambertMaterial (roughness≥0.9); no grid.glsl floor. Plus standard shared parts + 5-file contract layer.",
      "files": [],
      "expectations": [
        "Spec passes validate_spec.py with style='realistic'",
        "Generated renderer sets outputColorSpace = SRGBColorSpace and DPR ≤ 2 (v1.8)",
        "Generated scene does NOT call PMREMGenerator.fromScene(new Sky()) at runtime — uses a pre-baked HDR / RoomEnvironment (v1.8)",
        "DirectionalLight color/intensity come from tokens.lights.sun/sunIntensity, not a hardcoded 0xfff4e0 hex (v1.8)",
        "Concrete building bodies and roads use MeshLambertMaterial (not MeshStandardMaterial) where roughness≥0.9 & metalness=0 (v1.8)",
        "The ~0.05 'highlight convergence' bloom is NOT present (v1.8: removed as invisible under ACES)",
        "Grid shader floor is NOT wired (realistic skips grid.glsl); shadow camera ortho bounds derived from boundary",
        "5-file IDigitalTwinApi contract layer present; GlobalTwin.vue uses Promise.allSettled hydration (v1.8)"
      ]
    },
    {
      "id": 8,
      "name": "night-realistic-pointlight-budget",
      "prompt": "做一个夜间写实风格的园区数字孪生——夜景、窗户亮灯、路灯氛围。园区比较大，4 栋楼加长园区道路，路灯会比较多。帮我确保不会卡。",
      "expected_output": "A night-realistic scene that respects the v1.8 PointLight performance budget: ≤8 PointLights total, all castShadow=false; street lamps are emissive Sprite glows + 1-2 shared hero PointLights; car lights are emissive spheres (no real lights); pre-baked reflection texture (not MeshReflectorMaterial); window emissive baked into facade CanvasTexture once.",
      "files": [],
      "expectations": [
        "Generated scene has at most 8 PointLight instances total and ALL have castShadow=false (v1.8)",
        "Street lamps use emissive Sprite glow + at most 1-2 shared 'hero' PointLights, NOT one PointLight per lamp (v1.8)",
        "Car head/tail lights are emissive spheres, NOT real PointLight sources (v1.8)",
        "Ground uses a pre-baked blurred reflection texture, NOT MeshReflectorMaterial (v1.8)",
        "Window emissive texture is baked once into the facade CanvasTexture, not recomputed per frame (v1.8)",
        "PointLight color/intensity/distance come from tokens.lights.point/pointIntensity/pointDistance (v1.8)",
        "Hydration uses Promise.allSettled so a POI failure doesn't kill building hydration (v1.8)"
      ]
    },
    {
      "id": 9,
      "name": "holographic-no-transmission",
      "prompt": "做一个全息未来科技风的园区数字孪生——半透青玻璃楼栋、边缘辉光、bloom。要保证性能，别用太重的材质。",
      "expected_output": "A holographic scene using the v1.8 cheap recipe: translucent MeshStandardMaterial + emissive edges + bloom, with Fresnel rim via onBeforeCompile (assets/fresnelRim.glsl). MeshPhysicalMaterial transmission is explicitly NOT used.",
      "files": [],
      "expectations": [
        "Generated building material does NOT use MeshPhysicalMaterial or transmission property (v1.8 DO-NOT-USE)",
        "Fresnel rim glow (if used) is injected via onBeforeCompile referencing assets/fresnelRim.glsl, not via transmission (v1.8)",
        "No PMREM/HDR environment (holographic keeps translucency clean); lights from tokens.lights (hemiSky/hemiGround/ambient)",
        "outputColorSpace = SRGB and bloom present at a visible strength (~0.3), optionally selective bloom (v1.8)"
      ]
    },
    {
      "id": 10,
      "name": "white-model-park",
      "prompt": "给『城市展览馆』做个数字孪生沙盘——博物馆级的全白磨砂模型、柔和接地阴影、干净汇报风。3 栋楼 + 地下车库。",
      "expected_output": "A white-model scene: pure-white MeshLambertMaterial bodies, category color on roof edges only, PCFSoftShadowMap contact shadow, NO PMREM/HDR (v1.8 removed the white-Scene PMREM path).",
      "files": [],
      "expectations": [
        "Spec passes validate_spec.py with style='white-model'",
        "Building bodies use MeshLambertMaterial (not MeshStandardMaterial) — pure white (v1.8)",
        "Scene does NOT set up a PMREMGenerator.fromScene(white Scene) — only HemisphereLight + DirectionalLight (v1.8)",
        "Category color (building blue / garage green) appears on roof EdgesGeometry/Line2, not the body",
        "outputColorSpace = SRGB; DPR ≤ 2; legend swatches match tokens.category colors"
      ]
    },
    {
      "id": 11,
      "name": "isometric-park",
      "prompt": "做一个等距插画风的园区信息图——扁平 cel 着色的彩色楼栋、鲜活友好、适合汇报。3 栋楼。",
      "expected_output": "An isometric scene using MeshLambertMaterial with flatShading:true (v1.8), warm directional light camera-locked, NO shadows (v1.8 DO-NOT-ENABLE-SHADOWS), NoToneMapping.",
      "files": [],
      "expectations": [
        "Spec passes validate_spec.py with style='isometric'",
        "Building material is MeshLambertMaterial with flatShading:true (v1.8, NOT MeshStandardMaterial)",
        "shadowMap is disabled and there is an explicit DO-NOT-ENABLE-SHADOWS note (light is camera-locked) (v1.8)",
        "DirectionalLight color from tokens.lights.sun; NoToneMapping; outputColorSpace = SRGB"
      ]
    },
    {
      "id": 12,
      "name": "negative-malformed-spec",
      "prompt": "我整理了个园区规格，你先帮我校验一下有没有问题再生成。规格在 spec.json：两栋楼都标成了地下车库、有一栋楼没写楼层数、style 写成了 'neon'、有个 POI 的 buildingId 指向不存在的楼。",
      "expected_output": "Runs validate_spec.py and reports every FAIL/WARN WITHOUT generating the scene. The v1.8 validator must catch: 2 garages (at most 1), missing floors on a building-category entry, unknown style 'neon', and POI buildingId not matching any building. The assistant fixes each FAIL before proceeding.",
      "files": [],
      "expectations": [
        "Output runs scripts/validate_spec.py and reports exit code 1 with specific FAIL lines",
        "Validator flags the 2 garage-category buildings (expected at most 1)",
        "Validator flags the building-category entry missing 'floors' (v1.8 B1 — was previously silent)",
        "Validator flags style 'neon' as not in the 7-style enum",
        "Validator flags the POI buildingId that doesn't match any buildings[].id",
        "Assistant does NOT proceed to generate the scene until every FAIL is resolved"
      ]
    },
    {
      "id": 13,
      "name": "dynamic-data-failure-modes",
      "prompt": "数字孪生页面要能扛住后端异常：楼栋接口正常但 POI 接口挂了、快速连点不同楼层、某个楼层详情 404。帮我加上这些降级。",
      "expected_output": "Hardened hydration (v1.8): Promise.allSettled so POI failure doesn't kill buildings; AbortSignal + onCleanup on the floor-detail watch so rapid clicks don't race; floor-detail 404 shows inline error + retry in UnitDetail (not silently swallowed); unified ApiError so 401/404/5xx/timeout are distinguishable.",
      "files": [],
      "expectations": [
        "GlobalTwin.vue onMounted uses Promise.allSettled (not Promise.all) with per-method error state (v1.8)",
        "A POI request failing leaves buildings hydrated and interactive (not wiped) (v1.8)",
        "The floor-detail watch uses onCleanup + AbortController to cancel the previous in-flight request on rapid floor switching (v1.8)",
        "A 404 on getFloorDetail surfaces an inline error + retry in the UnitDetail panel, not a silent catch {} (v1.8)",
        "request<T>() supports a signal (AbortSignal) and timeout; network/timeout errors are wrapped in ApiError(0, ...) (v1.8)",
        "Mock and Real both throw ApiError (Mock 404 for missing floor), so error handling is mode-invariant (v1.8)"
      ]
    },
    {
      "id": 14,
      "name": "responsive-accessibility",
      "prompt": "这个数字孪生大屏要在不同屏幕和键盘用户下都能用——有的人用笔记本不是 4K 大屏，有的人只用键盘不用鼠标。帮我加上响应式和可访问性。",
      "expected_output": "Adds the v1.8 responsive + a11y layer: DPR ≤ 2; resize debounce; switcher role=tab + arrow-key nav; canvas aria-label; POI/UnitDetail focus management + Esc; prefers-reduced-motion disables tween & breathing; empty state when getBuildings() returns [].",
      "files": [],
      "expectations": [
        "renderer.setPixelRatio uses Math.min(window.devicePixelRatio, 2) (v1.8)",
        "Resize callbacks are debounced (~150ms), not run every frame (v1.8)",
        "BuildingSwitcher uses role='tab'/'tablist' + arrow-key navigation (v1.8)",
        "The canvas has role='img' + a descriptive aria-label (v1.8)",
        "POI popup and UnitDetail move focus in on open and restore it on Esc close (v1.8)",
        "prefers-reduced-motion: reduce disables the focus tween and POI alarm breathing animation (v1.8)",
        "getBuildings() returning [] shows an empty-state overlay with a retry button (v1.8)"
      ]
    },
    {
      "id": 15,
      "name": "webgl-context-loss-dispose",
      "prompt": "我们的数字孪生页面在切后台标签页回来或者显卡驱动休眠后偶尔变黑，切楼栋标签页时也偶尔卡。帮我修好生命周期。",
      "expected_output": "Adds v1.8 WebGL context loss handling and a full dispose checklist: webglcontextlost/restored listeners; stop RAF on loss + restore overlay; full dispose on unmount (RAF, ResizeObserver, OrbitControls, geometry/material/texture, composer, renderer.forceContextLoss); long-lived singleton surviving tab switches.",
      "files": [],
      "expectations": [
        "canvas listens to webglcontextlost (preventDefault + stop RAF + show restore overlay) and webglcontextrestored (rebuild GPU resources + restart RAF) (v1.8)",
        "GlobalTwin.vue onBeforeUnmount calls a full scene.dispose() covering RAF, ResizeObserver.disconnect, OrbitControls.dispose, geometry/material/texture disposal, composer + renderer.dispose + forceContextLoss (v1.8)",
        "ParkScene is a long-lived singleton that survives BuildingSwitcher tab switches (not rebuilt per tab) (v1.8)",
        "The scene does NOT continue rendering after a context-lost event (would throw) (v1.8)"
      ]
    },
    {
      "id": 16,
      "name": "backend-contract-envelope-rejection",
      "prompt": "后端同学说他返回的数据包了一层 { code, message, data }，前端直接读 data 就行。数字孪生前端契约这边怎么处理？",
      "expected_output": "Explains the envelope-absence rule and confirms the contract: the digital twin GET endpoints return the entity/array directly (no {code,message,data}); if a backend stub wraps in an envelope, the frontend types (snake_case direct fields) would mismatch and the Real implementation must NOT silently unwrap — it should push the backend to drop the envelope per net-api-developer / api-typescript-spec.",
      "files": [],
      "expectations": [
        "Output states the digital twin endpoints return entities/arrays directly with NO {code,message,data} envelope (aligned with api-typescript-spec)",
        "Output does NOT suggest silently unwrapping resp.data in the frontend (that would hide a backend contract violation)",
        "Backend DTO field names are snake_case and match the frontend types (BuildingRuntimeItem/FloorDetail/PoiRuntimeItem) (v1.8 contract)",
        "Lists the 3 GET endpoints with route, QueryMap, *Map DTO, permission, Controller (front-first)"
      ]
    }
  ]
}
