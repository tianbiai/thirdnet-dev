{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@bytebase/dbhub",
        "--dsn",
        "<由 thirdnet-mcp-setup 技能从 appsettings 读出、用户确认的 PostgreSQL 连接串；含真实账密→须 gitignore>"
      ],
      "env": {}
    }
  }
}
