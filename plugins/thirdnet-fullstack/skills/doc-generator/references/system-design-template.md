# [项目名称] 系统设计文档

> 系统级**设计**说明：以功能划分、业务流程与设计理念为主线，描述系统"做什么、怎么流转、为什么这样设计"。
> 本文档面向技术评审、架构归档与新人理解业务，**不堆砌代码级细节**（表名、字段类型、索引、路由、DTO 类型、权限串等属于 `spec.md` 开发文档）。占位符由 doc-generator 填充。

**文档版本**：[v1.0]　**编写日期**：[YYYY-MM-DD]　**编写者**：[git user / 姓名]

---

## 1. 设计理念与目标

### 1.1 设计目标

[系统要解决的核心问题、达成的设计目标。如：松耦合的微服务划分、可演进的业务模型、统一的前后端契约。]

### 1.2 设计原则

- [原则1：如"领域驱动划分服务边界，避免跨服务强耦合"]
- [原则2：如"前端先行定义契约，后端按契约实现，保证两端一致"]
- [原则3：如"按需缓存、异步化，保障主流程响应性能"]

### 1.3 设计权衡

| 决策点 | 选择 | 取舍说明 |
|--------|------|---------|
| [如：认证方案] | [JWT 国密 + 多 scheme] | [为什么不用 IdentityServer：内部系统、轻量化] |
| [如：HTTP 方法] | [仅 GET/POST] | [网关限制，简化前端对接与安全管控] |

---

## 2. 系统架构

### 2.1 架构概览

[一段话描述整体架构形态：前后端分离、微服务划分、网关位置、关键依赖。]

```
[概念拓扑图占位：用文字框图或 mermaid 表达层级关系，不标端口/表名]
┌─────────┐  ┌─────────┐
│ Web 端  │  │ 小程序   │
└────┬────┘  └────┬────┘
     └─────┬──────┘
       [ 网关 ]
     ┌──┬──┴──┬──┐
 [服务A][服务B][认证服务]
     └──┬─────┬──┘
     [数据库][缓存]
```

### 2.2 服务划分

| 服务 | 职责定位 | 核心职责 |
|------|---------|---------|
| [服务A] | [定位] | [做什么] |
| 认证服务 | 统一认证授权 | 登录、令牌签发与校验、权限目录管理 |

---

## 3. 功能模块设计

> 每个业务模块作为一个二级章节，按"职责—功能—流程"组织。模块划分源自前端功能入口与后端业务能力的对照。

### 3.1 [模块1，如：通知管理]

- **模块职责**：[这个模块在业务中承担什么]
- **核心功能**：
  - [功能1：如"通知的创建、发布、查询、撤回"]
  - [功能2]
- **业务流程**：

  [用文字或简单流程图描述主流程，如：创建通知 → 选择接收范围 → 发布 → 推送 → 已读统计]

- **设计要点**：[该模块设计上值得说明的点，如状态流转、与其它模块的协作]

### 3.2 [模块2]

[同上结构复制]

---

## 4. 数据模型设计

> 只描述**核心数据对象及其关系**，不展开列级字段（字段细节见 `spec.md`）。

### 4.1 核心数据对象

| 数据对象 | 代表什么 | 关键属性概念 | 归属模块 |
|---------|---------|------------|---------|
| [对象1，如：通知] | [业务含义] | [如：标题、内容、类型、状态、接收范围、发布时间] | [模块] |
| [对象2] | [业务含义] | [关键属性] | [模块] |

### 4.2 对象关系

[用文字或概念关系图描述对象间关联，如：用户 ←(多对多)→ 角色；通知 →(一对多)→ 接收记录。]

```
[概念关系图占位：对象 + 关系基数，不标外键名]
```

### 4.3 字典与状态

| 字典/状态 | 取值 | 业务含义 |
|----------|------|---------|
| [如：通知类型] | 系统 / 业务 | [含义] |
| [如：通知状态] | 草稿 / 已发布 / 已撤回 | [状态流转说明] |

---

## 5. 接口设计

> 按功能分组说明接口"做什么、何时触发、输入输出概念"，**不写路由与类型签名**。后端仅提供查询与提交两类操作。

### 5.1 [模块1] 接口

| 接口功能 | 触发场景 | 输入概念 | 输出概念 |
|---------|---------|---------|---------|
| [如：查询通知列表] | [进入列表页 / 筛选] | [筛选条件 + 分页] | [通知条目集合] |
| [如：创建通知] | [点击保存] | [通知各项内容] | [创建结果] |
| [如：发布通知] | [点击发布] | [通知标识] | [发布结果] |
| [如：查看通知详情] | [点击行] | [通知标识] | [通知详情] |

### 5.2 [模块2] 接口

[同上结构复制]

---

## 6. 安全设计

### 6.1 认证

- **方式**：[概念说明，如"基于令牌的认证，采用国密算法；登录后签发令牌，后续请求携带"]
- **登录与刷新**：[业务流程，如"登录校验账号密码后签发令牌；令牌过期前可刷新"]
- **统一认证服务**：由认证服务集中负责，[其它服务依赖其校验结果]

### 6.2 授权

- **策略**：[概念说明，如"基于角色的细粒度权限控制，权限项随服务启动自动注册并同步到前端"]
- **角色与功能可见性**：见 [需求规格说明书 → 角色与权限] 或下方简表

| 角色 | 可见/可操作的主要功能 |
|------|---------------------|
| [角色1] | [功能概述] |

### 6.3 安全防护

- [x] 数据传输：生产环境强制加密通道
- [x] 数据存储：查询参数化，防注入
- [x] 输入校验：统一入口校验
- [x] 访问控制：细粒度权限拦截

---

## 7. 缓存与后台任务

### 7.1 缓存

> 按需缓存，讲清"缓存什么、为什么缓存、失效策略"，不写键名模式。

| 缓存内容 | 用途 | 失效策略 |
|---------|------|---------|
| [如：权限目录] | [减少重复查询] | [启动加载 + 变更刷新] |
| [如：字典数据] | [避免高频读库] | [定时刷新] |

### 7.2 后台任务

| 任务 | 业务用途 | 执行节奏 |
|------|---------|---------|
| [任务1] | [如：定时清理过期数据] | [如：每日凌晨] |

---

## 8. 部署与运行

### 8.1 部署形态

[容器 / 进程 / 节点拓扑概念说明，占位。]

### 8.2 外部依赖

| 依赖 | 用途 |
|------|------|
| 数据库 | 业务数据持久化 |
| 缓存 | 分布式缓存与会话/热数据 |
| 认证服务 | 统一认证授权 |

### 8.3 配置管理

[概念说明：配置分模板与本地真实值两层，敏感配置不入库；具体配置项见 `spec.md`。]

---

## 9. 设计权衡与约束

- [约束1：如"仅 GET/POST、统一字段命名"]
- [约束2：如"认证走内部认证服务"]
- [已知技术债 / 演进方向]

---

## 10. 备注

[扫描阶段发现的与既有 spec 差异、待确认的设计点。代码层细节（表结构/字段/路由）请参见开发文档 `spec.md`，不在本文档展开。]
