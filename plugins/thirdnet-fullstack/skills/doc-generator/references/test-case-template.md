# [项目名称] 测试用例文档

> 测试执行、回归与验收依据。用例以**业务行为**为粒度——围绕"用户能完成什么操作、系统应如何响应"组织。
> 边界 / 异常用例源自业务规则与字段约束；不堆砌路由与类型签名。占位符由 doc-generator 填充；用例行按实际功能点复制扩充。

**文档版本**：[v1.0]　**编写日期**：[YYYY-MM-DD]　**编写者**：[git user / 姓名]

---

## 1. 测试范围

### 1.1 测试目标

[本次测试覆盖的功能模块与目的。]

### 1.2 测试范围

| 范围 | 说明 |
|------|------|
| 包含 | [模块清单] |
| 不包含 | [明确排除项] |

### 1.3 测试环境

| 项 | 配置 |
|----|------|
| 后端 | [服务地址 / 版本] |
| 前端 | [Web 地址 / 小程序版本] |
| 数据库 | [PostgreSQL 版本] |
| 测试账号 | [角色与账号，如管理员 / 业务员各一] |

---

## 2. 功能测试用例

> 每条用例聚焦一个可观测的验证点。用例编号建议 `TC-{模块}-{序号}`。

| 用例编号 | 模块 | 用例标题 | 优先级 | 类型 | 前置条件 | 测试步骤 | 预期结果 |
|---------|------|---------|--------|------|---------|---------|---------|
| TC-[模块]-001 | [模块] | [正常新增] | P0 | 功能 | 已登录且有相应操作权限 | 1. 进入[页面] 2. 点击新增 3. 填写合法值 4. 保存 | 保存成功，列表出现新记录 |
| TC-[模块]-002 | [模块] | [必填校验] | P1 | 边界 | 同上 | 1. 新增时 [必填项] 留空 2. 保存 | 提示"[字段]不能为空"，不提交 |
| TC-[模块]-003 | [模块] | [长度 / 格式校验] | P1 | 边界 | 同上 | 1. [字段] 输入超长 / 非法格式 2. 保存 | 提示校验错误 |
| TC-[模块]-004 | [模块] | [枚举边界] | P1 | 边界 | 同上 | 1. [字段] 选择每个选项 | 各值均可正常保存与回显 |
| TC-[模块]-005 | [模块] | [无权限访问] | P1 | 异常 | 登录无该操作权限的账号 | 1. 进入[页面 / 点击按钮] | 入口不可见或操作被拒 |
| TC-[模块]-006 | [模块] | [删除二次确认] | P2 | 功能 | 列表有数据 | 1. 点击删除 | 弹出确认框；取消则不删除 |
| TC-[模块]-007 | [模块] | [分页 / 查询] | P1 | 功能 | 数据 > 1 页 | 1. 翻页 2. 按条件筛选 | 结果正确，分页信息正确 |

> 边界用例源自业务规则与字段约束（必填 / 长度 / 选项）；权限分支用例源自角色与功能的可见性。

---

## 3. 接口测试用例

> 以接口能力为单位描述测试场景（系统对外只提供查询与提交两类操作），关注输入场景与预期行为，不写路由与类型签名。

| 用例编号 | 接口能力 | 输入场景 | 预期行为 |
|---------|---------|---------|---------|
| TC-API-001 | [如：查询通知列表] | 合法的筛选条件与分页 | 正常返回结果集合 |
| TC-API-002 | [如：查询通知列表] | 缺少必填的筛选条件 | 提示参数缺失，不返回数据 |
| TC-API-003 | [如：创建通知] | 各字段合法 | 创建成功 |
| TC-API-004 | [如：创建通知] | 未登录调用 | 拒绝访问（未认证） |
| TC-API-005 | [如：修改通知] | 当前角色无修改权限 | 拒绝访问（无权限） |
| TC-API-006 | [如：删除通知] | 目标不存在 | 提示不存在 |

---

## 4. 测试矩阵

> 模块 × 用例数概览，便于评估覆盖度。

| 模块 | 功能用例 | 接口用例 | 合计 |
|------|---------|---------|------|
| [模块1] | [n] | [n] | [n] |
| **合计** | [n] | [n] | [n] |

---

## 5. 缺陷跟踪

| 缺陷编号 | 模块 | 描述 | 严重程度 | 状态 | 负责人 |
|---------|------|------|---------|------|--------|
| BUG-001 | [模块] | [描述] | 严重 / 一般 / 轻微 | 新建 / 修复 / 验证通过 | [姓名] |

---

## 6. 备注

[测试数据准备说明、待补充用例清单、扫描阶段发现的边界规则等。]
