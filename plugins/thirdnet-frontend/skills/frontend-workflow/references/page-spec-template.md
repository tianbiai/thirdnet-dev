# [页面名称]

> 页面级规格说明，与 `[页面名].vue` 一一对应。

## 必读技能文档

| 优先级 | 技能 | 适用场景 |
|--------|------|----------|
| ⭐⭐⭐ | `vue-best-practices` | Vue 组件设计原则 |
| ⭐⭐ | `/frontend-design` | 界面设计规范（全局技能） |

---

## 业务与功能

### 核心目标

[该页面解决什么问题？用户价值是什么？]

### 功能清单

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P0 | [功能1] | [描述] |
| P0 | [功能2] | [描述] |
| P1 | [功能3] | [描述] |

### 强制要求

> ⚠️ 页面右上角必须有帮助说明气泡（仅演示模式显示），点击显示页面说明。

**帮助气泡规范**（由 `MOCK_ENABLED` 控制）：
- **条件渲染**：使用 `v-if="isMockEnabled"`，演示模式显示，生产模式完全移除（DOM 中无残留）
- **位置**：页面可视区域右上角（固定定位）
- **图标**：`QuestionFilled` 或类似图标
- **交互**：点击弹出帮助内容（气泡/Popover/Drawer）
- **内容**：页面功能说明、业务流程、注意事项
- **安全**：禁止使用 `v-show`，必须用 `v-if` 确保生产环境无信息泄露

### 交互要点

| 触发条件 | 响应行为 |
|----------|----------|
| [交互1] | [响应] |
| [交互2] | [响应] |

---

## 内容规划

### 信息架构

```
[页面名称]
├── [模块A]
│   ├── [子模块A1]
│   └── [子模块A2]
├── [模块B]
└── [模块C]
```

### 数据来源

- **数据类型**：[列表/单据/配置/...]
- **数据源**：Mock 数据（`mock/data/{endpoint}/{模块名}.ts`）←→ API 接口（`api/modules/{endpoint}/{模块名}.ts`）
- **关键字段**：`field1`: [说明], `field2`: [说明]

#### API 接口规范

> 遵循 `api-typescript-spec` 技能规范和 Agent 规则9（API 策略工厂架构）
> - API 模块（`api/modules/{endpoint}/*.ts`）采用策略工厂模式：定义 `IXxxApi` 接口契约 + `RealXxxApi`（HTTP）+ `MockXxxApi`（本地数据）+ `createXxxApi()` 工厂函数
> - Mock 数据（`mock/data/{endpoint}/*.ts`）使用 `import type` 保证类型一致，枚举使用 `import` 引入
> - 路径 `/app/{模块名}/{操作}` 或 `/manager/{模块名}/{操作}`，直接返回数据或 `PaginatedResponse<T>`，禁止 `code` 字段
> - **字段命名强制 snake_case**：所有 API 入参、出参、Mock 数据的字段名必须使用 `snake_case`（如 `order_id`、`created_at`、`user_name`），与后端 DTO 保持一致，禁止使用 `camelCase`
> - 通过 `.env` 中 `VITE_MOCK=true/false` 无缝切换 Real/Mock，业务代码零修改

| 接口 | 方法 | 路径 | 入参 | 出参 | Mock 数据文件 |
|------|------|------|------|------|--------------|
| [接口1] | GET/POST | `/app/{module}/list` | `{ param_name }` | `{ field_name }` | `mock/data/app/{module}.ts` |
| [接口2] | GET/POST | `/manager/{module}/list` | `{ param_name }` | `{ field_name }` | `mock/data/manager/{module}.ts` |

---

## 布局与组件

> 必须遵循 `vue-best-practices` 核心原则：单一数据源、Props down/Events up、组件小型化。

### 整体布局

- **布局模式**：[单栏/双栏/网格/自由]
- **容器宽度**：[固定/流式/混合]

### 组件架构

```
[页面名].vue
├── [子组件A].vue    # 职责：[描述]
├── [子组件B].vue    # 职责：[描述]
└── composables/
    ├── use[A].ts    # 职责：[描述]
    └── use[B].ts    # 职责：[描述]
```

**组件拆分检查**：
- [ ] 已拆分为小型、专注的组件
- [ ] 复杂逻辑已提取到 composables
- [ ] UI 部分已拆分为子组件（props in, events out）
- [ ] 所有 `.vue` 文件不超过 300 行（详见 [sfc-large-component-refactoring](sfc-large-component-refactoring.md)）
- [ ] 父组件是薄组合层，无业务逻辑

### 响应式适配

> uniapp 项目必须以 H5 模式开发和验证，代码需兼容微信小程序（mp-weixin）

- **桌面端（≥1200px）**：[布局描述]
- **平板端（768-1199px）**：[布局调整]
- **移动端（<768px）**：[布局调整]

---

## 视觉规范

> 必须遵循 `/frontend-design` 全局技能规范，避免通用 AI 美学。

### 设计风格

- **设计目的**：[这个界面解决什么问题？]
- **设计基调**：[极简主义/极繁主义/复古未来主义/...]
- **差异化亮点**：[什么让这个设计难以忘怀？]

### 严禁使用

> 同子项目 spec.md 设计系统的"严禁使用"规范（通用字体、紫色渐变、预测性布局等）

### 组件状态

| 状态 | 描述 |
|------|------|
| 默认态 | [描述] |
| 悬停态 | [描述] |
| 激活态 | [描述] |
| 禁用态 | [描述] |
| 加载态 | [描述] |

### 动画效果

- **页面加载**：[描述]
- **列表过渡**：[描述]
- **悬停微交互**：[描述]
