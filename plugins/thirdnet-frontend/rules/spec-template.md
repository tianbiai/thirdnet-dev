# [元素/页面名称]

## 📚 适用的技能文档清单

在编写本规格说明书时，请遵循 **`rules/skills-checklist.md`** 中列出的技能文档。

**核心技能**（必须阅读）：

- `skills/vue-best-practices/SKILL.md` ⭐⭐⭐ - Vue 3 核心最佳实践
- `skills/frontend-design/SKILL.md` ⭐⭐ - 前端界面设计规范

**完整技能清单**：参见 [skills-checklist.md](./skills-checklist.md)

## 📋 业务与功能

### 1.1 核心目标

> 简要说明该元素/页面的核心定位、解决的问题、用户价值

[在此描述核心目标...]

### 1.2 功能清单

> 列出所有功能模块及其优先级（P0: 必须实现 / P1: 重要 / P2: 可选）

- **P0 [功能模块1]**：[功能描述]
- **P0 [功能模块2]**：[功能描述]
- **P1 [功能模块3]**：[功能描述]
- **P2 [功能模块4]**：[功能描述]

### 1.3 强制要求（必须遵循）

> ⚠️ **强制要求**：页面可视化区域的右上角都必须有醒目的帮助说明气泡效果，点击显示页面说明信息。

**帮助说明气泡规范**：

- **位置**：页面可视区域的右上角（固定定位）
- **图标**：使用醒目的问号图标（`QuestionFilled` 或类似图标）
- **交互**：点击气泡显示帮助内容
- **显示方式**：气泡弹出、Popover 或 Drawer
- **内容滚动**：气泡内容显示区域应包含滚动条，若内容过多，可使用滚动条滚动查看
- **帮助内容**：
  - 页面功能说明
  - 业务流程说明
  - 其他注意事项

### 1.4 交互要点

> 关键的交互触发点、反馈机制、状态变化

- [交互点1]：[触发条件] → [响应行为]
- [交互点2]：[触发条件] → [响应行为]

---

## 📊 内容规划

### 2.1 信息架构

> 模块划分、信息层级、内容组织方式

```
[页面/元素名称]
├── [模块A]
│   ├── [子模块A1]
│   └── [子模块A2]
├── [模块B]
└── [模块C]
```

### 2.2 数据来源

> 数据源优先级：用户提供数据 > 项目数据表 > 生成示例数据

- **数据类型**：[列表/单据/配置/...]
- **数据源**：[用户提供/数据表路径/生成]
- **关键字段**：
  - `field1`: [字段说明]
  - `field2`: [字段说明]

### 2.3 内容示例

> （可选）重要的示例内容、文案语气、术语规范

[示例内容...]

---

## 🏗️ 布局与结构

> ⚠️ **必须遵循**：本部分设计必须严格基于 `vue-best-practices/SKILL.md` 中的组件设计原则和最佳实践。

### 3.1 整体布局

> 布局模式（单栏/双栏/网格/自由）、模块尺寸、比例约束

- **布局模式**：[单栏/双栏/网格/自由]
- **容器宽度**：[固定/流式/混合]
- **关键尺寸**：
  - [模块A]：[宽度/高度/比例]
  - [模块B]：[宽度/高度/比例]

### 3.2 组件架构设计

> ⚠️ **必须遵循 `vue-best-practices` 核心原则**：
>
> - **状态可预测性**：单一数据源，其他状态由此派生
> - **明确的数据流**：Props down, Events up（大部分情况）
> - **组件小型化**：优先使用小型组件 + composables，而非大型组件
> - **避免不必要的重渲染**：明智使用计算属性和侦听器

**组件拆分策略**：

- [ ] 页面/元素已拆分为多个小型、专注的组件
- [ ] 每个组件职责单一，易于测试、复用和维护
- [ ] 复杂逻辑已提取到 composables（`useXxx()`）
- [ ] UI 部分已拆分为子组件（props in, events out）
- [ ] 状态/副作用已移至 composables

**组件结构**：

```
[页面/元素名称].vue
├── [子组件A].vue (职责：[描述])
├── [子组件B].vue (职责：[描述])
├── [子组件C].vue (职责：[描述])
└── composables/
    ├── use[功能A].js (职责：[描述])
    └── use[功能B].js (职责：[描述])
```

### 3.3 响应式适配

> （如适用）断点、适配策略

**⚠️ uniapp H5 模式强制要求**：

- uniapp 项目必须以 **H5 模式**进行开发和验证
- 通过 `npm run dev:h5` 在浏览器中访问和测试
- 所有功能必须在浏览器 H5 模式下完整可用
- 微信等多端兼容使用条件编译语法（`#ifdef MP-WEIXIN`、`#ifndef H5`）实现
- 条件编译代码不得影响 H5 主代码逻辑
- **桌面端（≥1200px）**：[布局描述]
- **平板端（768-1199px）**：[布局调整]
- **移动端（<768px）**：[布局调整]

---

## 🎨 视觉规范

> ⚠️ **必须遵循**：本部分设计必须严格基于 `frontend-design/SKILL.md` 中的前端设计规范，避免通用 AI 美学。

### 4.1 设计规范来源

> 优先级：用户提供的设计规范 > 内置设计指导（基于技能文档）

**本设计基于**：

- [ ] 用户提供的设计规范：[路径/说明]
- [ ] **`frontend-design/SKILL.md`** - 前端界面设计规范 ⭐⭐
  - [ ] **设计思维**：明确设计目的、设计基调、约束条件、差异化亮点
  - [ ] **字体选择**：已选择独特、美观、有趣的字体组合，避免 Inter、Roboto、Arial 等通用字体
  - [ ] **色彩方案**：已承诺使用一致的配色方案（使用 CSS 变量），避免陈词滥调的紫色渐变
  - [ ] **动效设计**：已考虑动画效果和微交互（CSS 优先）
  - [ ] **空间布局**：已创造意想不到的布局（非对称、重叠、对角线流动、网格破坏）
  - [ ] **视觉细节**：已添加背景和视觉细节（渐变网格、噪点纹理、几何图案、阴影、边框等）

### 4.2 设计风格定位

> ⚠️ **必须基于 `frontend-design/SKILL.md` 的设计思维原则**

**设计风格选择**（选择一个明确的美学方向并精确执行）：

- [ ] **设计目的**：[这个界面解决什么问题？谁使用它？]
- [ ] **设计基调**：[选择一个极端方向：极简主义/极繁主义/复古未来主义/有机自然主义/奢华精致/玩趣玩具感/编辑杂志风/野兽派/装饰艺术/柔和粉彩/工业实用主义等]
- [ ] **约束条件**：[技术要求：框架、性能、可访问性]
- [ ] **差异化亮点**：[什么让这个设计难以忘怀？人们会记住哪一点？]

**⚠️ 严禁使用的设计元素**（基于 `frontend-design/SKILL.md`）：

- [X] 已避免使用通用字体（Inter, Roboto, Arial, 系统字体）
- [X] 已避免使用陈词滥调的配色方案（尤其是白色背景上的紫色渐变）
- [X] 已避免使用预测性的布局和组件模式
- [X] 已避免使用缺乏特定个性的千篇一律设计

### 4.3 自定义设计要点

> （可选）仅在有特殊的自定义设计要求时填写，如特殊色彩、特殊尺寸等

**自定义色彩**（如有）：

- [自定义色1]：[色值] - [用途]

**自定义尺寸**（如有）：

- [自定义尺寸1]：[值] - [用途]

**其他自定义规范**（如有）：

- [说明...]

### 4.4 组件状态

> 交互元素的状态定义

- **默认态（default）**：[描述]
- **悬停态（hover）**：[描述]
- **激活态（active）**：[描述]
- **禁用态（disabled）**：[描述]
- **加载态（loading）**：[描述，如适用]
- **错误态（error）**：[描述，如适用]

### 4.5 动画与过渡效果

> （如适用）基于 `vue-best-practices/SKILL.md` 中的 Transition 和 Animation 指导

- **页面加载动画**：[描述]
- **列表项过渡**：[描述]
- **悬停微交互**：[描述]
- **状态变化过渡**：[描述]
- **其他动画效果**：[描述]
